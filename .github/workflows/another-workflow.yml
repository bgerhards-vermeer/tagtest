name: Just Another GHA

on:
  workflow_call:
    inputs:
      a-number:
        required: true
        type: number
    
jobs:
  trying-splitting-steps:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
      - name: Failing maybe
        if: matrix.version == '9'
        run: exit 1
      - run: echo hello